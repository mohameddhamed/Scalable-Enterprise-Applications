CREATE TABLE EMPLOYEE (ID INTEGER NOT NULL, NAME VARCHAR(100), dept_id INTEGER NOT NULL, PRIMARY KEY (ID))
CREATE TABLE PARKINGLOT (EMPLOYEE_ID INTEGER NOT NULL, LOCATION VARCHAR(255), PRIMARY KEY (EMPLOYEE_ID))
CREATE TABLE DEPARTMENT (ID INTEGER NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE MEETING (ID INTEGER NOT NULL, finish_stamp TIMESTAMP, start_stamp TIMESTAMP, TITLE VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE scheduling (emp_id INTEGER NOT NULL  /*  */ , met_id INTEGER NOT NULL  /*  */ , PRIMARY KEY (emp_id, met_id))
ALTER TABLE EMPLOYEE ADD CONSTRAINT EMPLOYEE_dept_id FOREIGN KEY (dept_id) REFERENCES DEPARTMENT (ID)
ALTER TABLE PARKINGLOT ADD CONSTRAINT PRKINGLOTMPLOYEEID FOREIGN KEY (EMPLOYEE_ID) REFERENCES EMPLOYEE (ID)
ALTER TABLE scheduling ADD CONSTRAINT scheduling_met_id FOREIGN KEY (met_id) REFERENCES MEETING (ID)
ALTER TABLE scheduling ADD CONSTRAINT scheduling_emp_id FOREIGN KEY (emp_id) REFERENCES EMPLOYEE (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
